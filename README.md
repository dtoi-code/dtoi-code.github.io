请帮我做以下修改，严格保持页面所有样式、颜色、字体、布局、内容完全不变，只修改导航栏的实现方式：

1. 网站根目录已经有一个 nav-config.js，里面统一配置导航栏菜单：
const navConfig = [
  { name: "主页", path: "/" },
  { name: "项目", path: "/project/" },
];
这个JS会自动生成导航、自动高亮当前页active、自动渲染主题切换按钮。

2. 我需要你把当前HTML页面中原有的手写导航栏代码：
<div class="nav">...</div>
整段删除，替换成空的：
<div class="nav"></div>

3. 然后在页面 </body> 结束前引入：
<script src="/nav-config.js"></script>

4. 删除页面中原来手写的导航相关JS：包括主题切换代码、goHome()、openProject() 等导航跳转函数，只保留页面自身业务功能代码。

5. 绝对不能修改任何CSS样式、任何页面内容、文字、颜色、字体、间距、位置、布局、深色模式、高亮效果，所有视觉必须和原来完全一样。

6. 最终效果：导航栏外观、行为、高亮、主题切换完全不变，但由 nav-config.js 统一控制菜单。